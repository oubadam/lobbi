# Optional: leave unset for demo mode (fake trades, no Solana)
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# WALLET_PRIVATE_KEY=base58_private_key_here

# Override paths (default: repo root config/ and data/)
# CONFIG_DIR=/path/to/config
# DATA_DIR=/path/to/data
