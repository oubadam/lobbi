# Railway/Nixpacks: install full deps (including devDependencies) so tsc is available
[phases.install]
cmds = ["npm install", "npm install --prefix clawdbot", "npm install --prefix backend", "npm install --prefix web"]
